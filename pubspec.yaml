name: bateria_application
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  sensors_plus: ^7.0.0
  battery_plus: ^5.0.0
  device_info_plus: ^12.0.0
  geolocator: ^9.0.0
  camera: ^0.10.0
  permission_handler: ^11.4.0
  vibration: ^3.1.4
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true